# Swift build products (see also swift/.gitignore)
.build/
build/
.swiftpm/
Package.resolved

# Leftovers from the removed Tauri build, and anything a stray `npm install` in the root
# (package.json is still here as semantic-release's version anchor) would drop.
node_modules/
dist/
src-tauri/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
